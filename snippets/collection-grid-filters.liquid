{% comment %}
- collection: Liquid 'collection' or 'search' object
- enable_sidebar: boolean
- filter_style: 'sidebar' or 'drawer'
- enable_color_swatches: boolean
- collapsed: boolean

 $('#SortBy').parent().append('<ul id="newyearfilter" name="SortBy"></ul>');

  var url = window.location.href;
  var term = "?filter.p";
  var term2 = "?sort_by";var term3 = "&sort_by";


  $('#SortBy option').each(function(){
    if (window.location.href.indexOf("?filter.p") > -1 && window.location.href.indexOf("&sort_by") > -1) {
      var data = url.split("?");
      var word_id = data[1];
      $('#newyearfilter').append('<li value="' + $(this).val() + '"><a href="'+url+'&sort_by='+$(this).val()+'">'+$(this).text()+'</a></li>');
    } else if( url.indexOf( term2 ) != -1 ) {

    } else {
      $('#newyearfilter').append('<li value="' + $(this).val() + '"><a href="'+url+'?sort_by='+$(this).val()+'">'+$(this).text()+'</a></li>');
    }
  });
  $('#SortBy').hide();
  $('#newyearfilter').attr('id', 'SortBy');



 //  $('.sortbyul__list li').on('click', function(){
	//     var value = $(this).attr("rel");alert(window.location.search);
	//     $('#SortBy').find('option[value="'+value+'"]').prop('selected',true).trigger('change');
	//     return false;
	// });
{% endcomment %}

{%- assign animation_row = 1 -%}

{%- if enable_sidebar -%}
<div id="CollectionSidebar" data-style="{{ filter_style }}">
    {%- if filter_style == 'sidebar' -%}
    <div class="collection-sidebar small--hide">
        {%- render 'collection-grid-filters-form', location: 'CollectionSidebar', filters: collection.filters, collapsed: collapsed, enable_color_swatches: enable_color_swatches -%}
    </div>
    {%- endif -%}  
    {%- if filter_style == 'horizontal' -%}
    <div class="collection-horizontal small--hide">
        {%- render 'collection-grid-filters-form', location: 'CollectionSidebar', filters: collection.filters, collapsed: collapsed, enable_color_swatches: enable_color_swatches -%}
    </div>
    {%- endif -%}  

    <div id="FilterDrawer" class="drawer drawer--left">
      <div class="drawer__contents">
        <div class="drawer__fixed-header">
          <div class="drawer__header appear-animation appear-delay-{{ animation_row }}">
            <div class="h2 drawer__title">
              {{ 'collections.filters.title_tags' | t }}
              {% assign active_filters_count = 0 %}
              {% for filter in collection.filters %}
                {% assign active_filters_count = active_filters_count | plus: filter.active_values.size %}
              {% endfor %}

              <a href="{{ collection.url }}"
                class="clear-all-filters{% if active_filters_count == 0 %} hidden{% endif %}">
                Clear All
              </a>
            </div>
            <div class="drawer__close">
              <button type="button" class="drawer__close-button js-drawer-close custom_DrawerClose">
                <svg aria-hidden="true" focusable="false" role="presentation" class="icon icon-close" viewBox="0 0 64 64"><title>icon-X</title><path d="m19 17.61 27.12 27.13m0-27.12L19 44.74"/></svg>
                <span class="icon__fallback-text">{{ 'general.drawers.close_menu' | t }}</span>
              </button>
            </div>
          </div>
        </div>

        {%- assign animation_row = animation_row | plus: 1 -%}
        <div class="drawer__scrollable appear-animation appear-delay-{{ animation_row }}">
          {%- render 'collection-grid-filters-form', location: 'SidebarDrawer', filters: collection.filters, collapsed: collapsed, enable_color_swatches: enable_color_swatches -%}
        </div>
      </div>
    </div>            
  </div>
{%- endif -%}